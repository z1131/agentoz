name: Publish agentoz-starter

on:
  push:
    branches: [main, master]
    paths:
      - '.github/workflows/publish-sdk.yml'
      - 'agentoz-starter/**'
      - 'pom.xml'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          server-id: github
          settings-path: ${{ github.workspace }}

      - name: Publish agentoz-starter to GitHub Packages
        run: |
          # 先安装父 POM 和依赖到本地仓库
          mvn install -DskipTests -s ${{ github.workspace }}/settings.xml
          # 只发布 agentoz-starter 模块
          mvn deploy -DskipTests -s ${{ github.workspace }}/settings.xml -pl agentoz-starter
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
