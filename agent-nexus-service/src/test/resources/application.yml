server:
  port: 8080

spring:
  application:
    name: agent-platform-test

  # 测试时禁用 Nacos
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false

  datasource:
    url: jdbc:mysql://rm-bp1tbjfarup52kbpbao.mysql.rds.aliyuncs.com:3306/agent?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: agent
    password: Aa1231231212123
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 2
      min-idle: 2
      max-active: 10

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms

# Dubbo 测试配置
dubbo:
  application:
    name: agent-platform-test
    qos-enable: false
  registry:
    address: N/A
  protocol:
    name: dubbo
    port: -1
  scan:
    base-packages: com.deepknow.platform.infrastructure.rpc

# MyBatis Plus
mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.deepknow.platform.domain.*.model
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: none
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    com.deepknow: DEBUG
    com.baomidou.mybatisplus: DEBUG
